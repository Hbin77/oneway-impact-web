# PRD — oneway-impact-web (가제)

> 출처: Hermes 세션 20260911_132905_ab066b (B 단계 수정 PRD + C-계획 승인분). 문장은 세션 기록 원문 그대로이며, 아래 두 절은 승인 시점의 정정 사항이다.

## B. PRD — oneway-impact-web (가제)

**1) 한 줄 정의**  
지자체 교통 담당자가 지명만 입력하면, 용역 발주 전에 일방통행 전환 후보 구간·통행시간 영향·상권 영향을 근거와 함께 빠르게 1차 검토하는 웹 서비스.

**2) 타깃 사용자**  
지자체 교통과·도로과·도시계획 실무자(B2G). 2차로 관심 주민도 쓸 수 있지만, MVP 주 소비는 담당자. 결과를 보고서·민원 답변·회의 자료에 바로 붙일 수 있어야 한다.

**3) 문제**  
일방통행 전환 검토는 수개월·수천만 원 용역이 필요해서 실제로는 민원·경험칙으로 결정된다. 도로를 바꾸면 운전자가 경로를 다시 고르고, 구간 하나를 바꾸면 나머지 구간 교통량이 전부 달라진다. 사람이 손으로 못 해 지금까지 안 했다. 일방통행은 표지판·노면표시만 바꾸면 돼 실행 가능한 유일한 축인데, 근거가 없어 실행으로 못 가는 경우가 많다.

**4) 트리거**  
“이번 분기/올해 TSM 후보 좀 찾아볼까” 할 때, 또는 특정 민원·건의에 답을 줘야 할 때. 용역 발주 전 자체 스크리닝 용도. 공유 링크로 바로 꺼내 쓰는 흐름이 자연스럽다.

**5) 핵심 기능**

- P0 (최대 4개)
  1. 지명 기반 분석 실행 — 지명 입력 → Google 지오코딩 → 위경도(bbox) → 도로망 분석 → 결과
  2. 조합 최적안 — 몇 개 구간을 어떤 순서로 바꾸면 누적 몇 % 개선되는지
  3. 브라에스 역설 구간 표 — 막으면 전체가 좋아지는 구간
  4. 절대 막으면 안 되는 구간 표 — 막으면 통행시간이 폭증하는 구간
     ※ 상권 영향은 실무상 필수지만 P0 슬롯 4개 규칙에 맞춰 위 4개로 압축했다.

- P0 (분석 결과 소비 레이어 — P0 슬롯 4개와는 별개)
  5. Solar 에이전트 질의응답 — 분석 결과(엔진 마크다운)를 Solar Pro 4가 읽고 사용자 질문에 근거와 함께 답변(구조화). engine job_id를 근거로 표시한다. 회의·민원 답변·제안 문서에 바로 붙일 수 있게 하는 결과 소비 레이어다.

- P1
  1. 상권 영향 — 손해 보는 상가 유무, 최악 상가 변화분
  2. 체감 환산 — “매일 N시간 단축”처럼 % 개선을 체감 단위로 번역
  3. 표현층 개선(선택) — 3D 도로망·애니메이션·데이터 시각화. MVP 제출본에서는 P0에 넣지 않고, UI 품질 목표로만 둔다. 구현 범위는 C 단계에서 확정.
  4. 신고 접수 → 1차 분류 → 분석 대상 후보 모집(향후 계획): 일반 시민의 도로 불편· 비효율 신고를 접수하되, 현재 서비스 범위에서 즉시 분석하지 않는다. 접수한 신고는 일방통행 전환 검토에 적합한 유형인지 1차 분류한 뒤 분석 대상 후보로 모은다. 신호·버스노선·주차 문제 등 제외 범위는 이 단계에서 걸러서 별도 안내한다. 후보 모집 결과는 담당자에 대한 분석 추천 자료로 쓰며, 실제 분석 실행과 시행 결정은 각각 별도 절차다. 대상과 협업 범위는 후속 검토 항목이다.

- 표현층 방향(참고)만 남긴다: anime.js·Motion·Bklit 계열로 표/카드 등장 애니메이션, 개선율 강조, 보조 차트 등을 검토할 수 있다. 3D 장면은 좌표 데이터 추출, 구역별 도로망 밀도, 처리 시간 문제로 MVP 필수 범위에서 뺀다.

**6) 스킬 매핑**

- 사용 스킬: `oneway-impact-analyzer` (원본: `scripts/road_impact.py`, Overpass 도로망 수집 + BPR + Frank-Wolfe UE 배정 + 방향 차단 재배정 + 조합 탐색 + 상권 영향 평가).
- 호출 방식(그대로 호출): 웹의 `/api` 서버리스 함수(Vercel Python)에서 원본 스크립트를 재구현 없이 그대로 호출한다. 재구현은 하지 않는다.
- 변경·고도화 내역:
  - 등록 시 SKILL.md 머리말을 Hermes 규격(`name`, `description` 등)으로 조정했다.
  - `/api` 이식 방식: Python 런타임에서 스크립트를 실행하고, 결과를 마크다운/JSON으로 반환. 3D용 좌표 데이터를 따로 꺼내는 구조는 MVP 필수 범위에서 제외한다.
- Solar 에이전트 매핑: SKILL.md에 적힌 분석 절차와 고지사항(해석 주의, 합성 OD, 조합 최적화 성격 등)을 Solar 시스템 프롬프트로 그대로 사용한다. Solar는 원본 스킬의 산출물(엔진 마크다운)을 읽고 설명·구조화만 하며, 수치를 새로 만들거나 바꾸지 않는다.

**7) 입력·출력**

- 입력: 지명 텍스트 1개. Google Maps Geocoding API로 위경도(bbox) 변환. 해석 실패/애매 시 후보 제시·재질문·수동 bbox fallback.
- 출력(두 형식, 내용 동일):
  - 화면: 조합 최적안 + 브라에스 표 + 막힘 금지 표 + 상권 영향 + 체감 환산 + 근거 고지
  - 다운로드: 동일 내용을 담은 마크다운/보고서 형태

**8) 필요 설정**

- 배포 연결(Vercel 커넥터, 값 취급 없음): Vercel 프로젝트 생성·연결, 배포 파이프라인.
- 서비스 키(Vercel 환경변수: 이름·용도·발급처만, 값 금지):
  - `GOOGLE_MAPS_GEOCODING_API_KEY` — 지명 → 위경도 지오코딩용 (Google Maps Geocoding API 발급, 결제 카드 등록·과금 필요). 한국 지명 정확도 향상을 위해 요청에 `region=kr`, `language=ko` 파라미터를 함께 보낸다. 서버에서만 사용, 클라이언트 소스·응답 JSON에 노출 금지.
  - `UPSTAGE_API_KEY` — Solar Pro 4 chat completions 호출용 (Upstage 콘솔에서 발급). 서버 /api에서만 사용, 클라이언트 소스·응답 JSON에 노출 금지. 엔드포인트: `https://api.upstage.ai/v1/chat/completions` (OpenAI 호환). 모델은 `upstage/solar-pro4` 하나만 사용하며 다른 모델·폴백·MoA는 쓰지 않는다.
  - `DATA_GO_KR_KEY` — 전국일방통행도로표준데이터(공공데이터포털) 조회용. 경찰청·행정안전부가 공개하는 일방통행도로 표준데이터(도로명, 구간, 지정연도, 차로수 등)를 공공데이터포털 API로 조회할 때 사용한다. 서버 /api에서만 사용, 클라이언트 소스·응답 JSON에 노출 금지.
  - `ABST_API` — A/B Street headless 서버 베이스 URL (예: `http://127.0.0.1:1234`). 순천 구역 분석 시 엔진 마크다운을 A/B Street 시뮬레이션에 넣어 전환 전/후 비교를 수행한다. 서버 /api에서만 사용, 클라이언트 소스·응답 JSON에 노출 금지.

- 사용자 프로필: 이번 MVP는 최소화. “지명만 넣으면 바로 분석”이 기본. 반복 사용용 설정은 이후 확장으로 남긴다.

**9) 데이터 규칙**

- 출처·기준 시각 표기, 없는 값은 ‘확인 불가’, 추정 금지.
- 키·프록시 주소는 서버(/api)에만, 최소 권한.
- 일방통행 표준데이터(경찰청 전국일방통행도로표준데이터, 공공데이터포털)는 근거 제시용으로만 쓰고 엔진 계산에는 사용하지 않는다. 출처 표기: 경찰청 전국일방통행도로표준데이터(공공데이터포털, CC BY).
- OD 통행량은 합성값 → 절대 시간이 아니라 구간 간 상대 비교로만 유효.
- 보행 안전·형평성은 계산 범위에 없음. 통행시간·상권 접근성 두 축만 본 결과.
- LLM 출력(Solar 응답)은 엔진 수치를 바꾸지 않는다. 수치·좌표는 엔진 보고서에서만 인용하며, Solar는 설명·구조화만 한다.
- 사용 라이브러리(anime.js, Motion, Bklit)의 라이선스(MIT) 표기 포함.

**10) 제약**

- Vercel 서버리스 함수 1요청 최대 5분, 월 4 CPU-시간 범위 안에서 운영한다.
- Overpass 미러 전부 실패 시 분석 중단(수동 재시도 안내). 순천 원도심은 동봉 캐시로 대체 가능하지만, 그 외 구역은 실측 데이터 확보 실패 시 중단한다.
- 해외 구역은 실측 교통량 데이터가 없어 OSM 기반 추정치만 사용한다.
- Google Geocoding은 결제 카드 등록·과금이 필요하며, 배포·운영 비용은 본인 부담이다.

**11) 성공 기준 (데모 시나리오, 시크릿 창 테스트 포함)**

- 시크릿 창(저장값 없는 첫 방문)에서 지명 입력 화면이 보이고, 입력 후 분석 진행 → 결과 표/카드 + 근거 고지가 표시된다.
- 다운로드 보고서가 생성된다.
- 분석 1회가 5분 안에 끝난다.
- Deployment Protection(Require Login)이 꺼져 있어, 계정 없이 공개 접속된다.
- `/api`·index 상태 코드 정상, 클라이언트 소스에 키·프록시 주소 0.

**12) 제외 범위**

- 보행 안전·형평성 평가
- 신호 주기·버스노선·주차 문제
- 신설 도로 타당성
- 실제 시행 여부 결정(의사결정이 아니라 근거 자료 제공)

**대응표(걸음 ①~⑤ → PRD 항목)**  
① WHO → 타깃 사용자 / ② WHEN → 트리거 / ③ INPUT·OUTPUT → 입력·출력 / ④ SETUP → 필요 설정 / ⑤ SOLVE → 한 줄 정의·문제·핵심 기능

---

---

# C-계획에서 정정된 항목 (이 내용이 위 본문보다 우선한다)

### 1) PRD 수정 반영 (B에서 가져온 것 중 고친 부분만)

**10) 제약 (수정)**  
- Vercel 서버리스 함수 1요청 최대 5분, 월 4 CPU-시간 범위 안에서 운영한다.  
- 순천 외 구역은 OSM 값 + 도로등급 기본값으로 계산한다. **중단은 Overpass 미러 4곳 전부 실패 때만**이다.  
- 해외 구역은 실측 교통량 데이터가 없어 OSM 기반 추정치만 사용한다.  
- Google Geocoding은 결제 카드 등록·과금이 필요하며, 배포·운영 비용은 본인 부담이다.

**11) 성공 기준 (추가)**  
- 시크릿 창에서 지명 입력 화면이 보이고, 입력 후 분석 진행 → 결과 표/카드 + 근거 고지가 표시된다.  
- 다운로드 보고서가 생성된다.  
- 분석 1회가 5분 안에 끝난다.  
- Deployment Protection(Require Login)이 꺼져 있어 계정 없이 공개 접속된다.  
- `/api`가 빈 값·지오코딩 실패 같은 이상한 입력에 대해 이유를 적어 4xx로 답한다.  
- `/api`·index 상태 코드 정상, 클라이언트 소스에 키·프록시 주소 0.

**표현층 / 스킬 매핑 (React 명시)**  
- 프론트엔드: React(클라이언트). 3D 장면은 Three.js 계열, 애니메이션은 anime.js 또는 Motion, 데이터 시각화는 Bklit/자체 차트 보조 검토 가능.  
- 스킬 매핑: `oneway-impact-analyzer`의 `scripts/road_impact.py`를 Vercel Python 서버리스 함수에서 그대로 호출. 재구현 없음.  
- 변경·고도화 내역: 등록 시 SKILL.md 머리말을 Hermes 규격으로 조정했고, `/api` 이식은 Python 런타임에서 스크립트 실행 후 마크다운/JSON 반환. 3D용 별도 좌표 데이터 추출 구조는 MVP 필수 범위에서 제외.

---

### 4) P0 요약

- 지명 입력 → Google 지오코딩 → 위경도 bbox → `/api` Python에서 원본 스킬 그대로 실행 → 결과 반환.  
- P0 4개: 조합 최적안 / 브라에스 역설 구간 표 / 절대 막으면 안 되는 구간 표 / 상권 영향.  
- P0 결과 소비 레이어(별개): Solar 에이전트 질의응답 — 엔진 마크다운을 Solar Pro 4가 읽고 사용자 질문에 근거와 함께 답변(구조화), engine job_id를 근거로 표시.  
- P1: 상권 영향(실제 표기는 P0 결과에 포함), 체감 환산, 전국 표준노드링크 실측 매칭(후속 고도화), 표현층 3D·모션·시각화는 MVP 필수 범위에서 제외하되 품질 목표로 남겨둔다.  
- 프론트엔드 React, `/api`는 Python 서버리스, 키는 서버 환경변수만, 클라이언트 노출 금지.

---
